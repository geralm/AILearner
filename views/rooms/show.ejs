<%layout('layouts/rooms', {room: response}) %>
    <div class="notes-section">
        <%-include('../partials/notes') %>
    </div>
    <div class="main-section container-fluid d-flex">
        <%- include('../partials/chat', { response: response });%>
    </div>